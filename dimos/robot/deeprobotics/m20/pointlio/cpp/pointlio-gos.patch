Point-LIO fixes required by the M20 GOS build.

--- a/src/laserMapping.hpp
+++ b/src/laserMapping.hpp
@@ -644,4 +644,4 @@
             /*** iterated state estimation ***/
-            crossmat_list.reserve(feats_down_size);
-            pbody_list.reserve(feats_down_size);
+            crossmat_list.resize(feats_down_size);
+            pbody_list.resize(feats_down_size);
             // pbody_ext_list.reserve(feats_down_size);
--- a/include/ivox/ivox3d.h
+++ b/include/ivox/ivox3d.h
@@ -8,2 +8 @@
-#include <glog/logging.h>
 // #include <execution>
