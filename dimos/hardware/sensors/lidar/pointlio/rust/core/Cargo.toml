[package]
name = "pointlio-core"
version = "0.1.0"
edition = "2021"
description = "Point-LIO (IMU-as-output LIO) estimator, ported from hku-mars/Point-LIO"
# Derived from GPL-2.0 upstream; lives in-tree for now, moves to its own repo later.
license = "GPL-2.0"

[dependencies]
nalgebra = "0.35.0"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
