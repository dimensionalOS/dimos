# Question Triage: m20-rosnav-migration

**Scope selected:** All (P0 + P1 + P2 + P3)
**Questions in scope:** 147
**Auto-answerable:** 95
**Branch points for human:** 52

---

## Auto-Answerable Questions

| # | Question | Proposed Answer | Source |
|---|----------|-----------------|--------|
| 1 | What single indicator tells the operator the robot is ready for its first mission? | A single composite "READY" state aggregating: (a) host dimos running, (b) Docker container healthy, (c) FASTLIO2 converged, (d) /ODOM publishing, (e) lidar healthy. Current entrypoint already checks /ODOM, /IMU, and /ALIGNED_POINTS sequentially before exec. The web UI should show a single green status badge once all subsystems report healthy. | `entrypoint.sh:34-42` (topic wait logic), `launch_nos.py:104-108` (prints "dimos running!" only after build), industry best practice (single readiness indicator) |
| 3 | Does the migration require a period of downtime where the robot cannot navigate at all, and if so, how long? | Yes. The architectural change (monolithic Docker to host+container split) cannot be done live. Downtime = time to install uv/Python 3.10 on NOS + pull new container image + first boot. Estimate: 30-60 minutes for initial setup, sub-5 minutes for subsequent restarts. The existing `deploy.sh` already handles stop/start cycles. | `deploy.sh` lifecycle commands, plan Phase 1 step 1 |
| 6 | Can users still teleoperate the robot when autonomous navigation is not working, and is the fallback identical to today? | Yes. M20Connection supports Regular Mode (UDP axis commands) independently of rclpy/ROS. If nav container dies, UDP heartbeat + axis commands still function. Teleop is identical -- same protocol, same normalized [-1,1] commands. The M20 dev guide confirms Regular Mode uses UDP on port 30000. | `connection.py:20-24` (dual mode), M20 dev guide Section 1.2.5 (axis commands), `velocity_controller.py` (UDP path) |
| 7 | What happens if the Docker container running the nav stack crashes mid-mission -- does the robot stop safely or continue driving blindly? | The robot stops. When the container dies, LCM `cmd_vel` messages stop flowing to M20Connection. The velocity controller has a watchdog: if no cmd_vel received within timeout, it sends zero velocity. Additionally, DockerModule has `docker_restart_policy: "on-failure:3"` which auto-restarts. The robot will not continue driving blindly. | `docker_runner.py:77` (restart policy), `velocity_controller.py` (watchdog timeout), M20 dev guide: robot requires continuous heartbeat commands |
| 17 | What happens if staff manually move the robot between floors outside the intended workflow (kidnapped robot scenario)? | FASTLIO2 (Phase 1) has no relocalization -- it will lose localization entirely. The system must detect this via SLAM divergence (large odometry jump) and enter a "LOST" state requiring operator to trigger re-mapping or manual position set. Phase 2's arise_slam adds relocalization which can handle this case. Standard SLAM behavior -- no ambiguity. | FASTLIO2 architecture (no loop closure/relocalization), plan Phase 2 |
| 19 | How will the user know if FASTLIO2 SLAM has lost localization -- and what is the distinction between degraded localization and total loss? | The CMU nav stack publishes `/registered_scan` and TF. When FASTLIO2 diverges, the registered scan will visually misalign with the map (visible in web UI). The system should monitor covariance/innovation metrics from the iEKF and expose a 3-level indicator: Good (green), Degraded (yellow, high uncertainty), Lost (red, no valid pose). This is standard practice in SLAM systems. | ROSNav subscribes to `/registered_scan` and `/tf` (`rosnav_docker.py:312-321`), iEKF-based FASTLIO2 design |
| 20 | What are all the top-level user-visible system states, and what distinct visual treatment does each require? | Based on NavigationState enum plus system lifecycle: BOOTING (grey, spinner), INITIALIZING (grey, progress bar), IDLE (blue, ready badge), MAPPING (purple, pulsing), NAVIGATING (green, animated path), REPLANNING (yellow, route recalculating), STUCK (orange, alert), ERROR (red, action required), RECOVERY (yellow, self-healing), TELEOP (cyan, manual control), SHUTTING_DOWN (grey, winding down). | `base.py:22-25` (IDLE, FOLLOWING_PATH, RECOVERY), `entrypoint.sh` boot phases, industry AMR state machines |
| 21 | What is the happy path for powering on the M20, waiting for ready, setting a goal, navigating, and arriving? | 1. Power on M20 (AOS/GOS/NOS boot ~60s). 2. NOS auto-starts dimos host + Docker container. 3. Entrypoint waits for /ODOM + /IMU + lidar health. 4. Web UI shows "Ready". 5. User taps map location or selects named waypoint. 6. ROSNav publishes /goal_pose to FAR planner. 7. FAR planner generates path, base_autonomy executes. 8. cmd_vel flows through ROSNav bridge to M20Connection to /NAV_CMD. 9. /goal_reached fires, UI shows "Arrived". | `entrypoint.sh` boot sequence, `rosnav_docker.py:466-504` (navigate_to flow), `launch_nos.py` blueprint |
| 23 | How does pause, resume, and cancel work mid-mission -- including the immediate visual feedback for emergency stop? | ROSNav exposes `stop_navigation()` RPC which publishes /cancel_goal (Bool) and /stop (Int8 value 2). Cancel is immediate via ROS topic. Resume = send new goal. The M20 hardware e-stop (HES) is independent and always available. UI must show e-stop button omnipresent (never occluded). | `rosnav_docker.py:506-529` (stop_navigation), M20 dev guide 1.3.1.1 (HES status) |
| 29 | Will the robot's top speed, acceleration profile, or responsiveness change after migration? | Speed limits are set in M20Connection's VelocityController and are independent of the nav stack. Current launch_nos.py sets max_linear_speed=1.0, max_angular_speed=1.2. The FAR planner may command different trajectories but the velocity caps are enforced at the M20Connection layer. Acceleration profiles depend on the M20 firmware (agile gait), not dimos. | `launch_nos.py:72-78` (speed config), `velocity_controller.py` (speed limiting) |
| 30 | Will the web visualization (WebVis/map/costmap/path display) look and behave the same? | The web UI (command center at port 7779) is served by host-side dimos. ROSNav publishes pointcloud and global_map via LCM to the same topics the current VoxelGridMapper uses. The visualization pipeline (RerunBridge, WebsocketVisModule) remains on the host, consuming the same LCM topics. Visual appearance should be identical or improved (FAR planner provides terrain_map_ext). | `launch_nos.py:65-99` (blueprint with rerun_bridge), `rosnav_docker.py:259-263` (output ports match spec) |
| 32 | Does the robot boot faster or slower with the new architecture, and what is the boot-to-ready time? | Slightly slower on first boot (Docker container pull + start adds ~10-30s). Subsequent boots: Docker start is ~5-10s. Current boot: NOS → Docker container start → entrypoint topic waits (~30s for /ODOM + /IMU) → lidar check (~15s) → ready. New boot adds: host Python startup (~3s) + container startup (~10s). Net: comparable, possibly +10-15s. Docker images are cached on NOS eMMC. | `entrypoint.sh` (30s topic waits), `docker_runner.py:41-46` (timeouts), `deploy.sh` pull/start flow |
| 35 | How does the user verify that the robot's localization is correct before starting a mission? | The web UI shows the robot's pose overlaid on the map. The user can visually confirm that the robot's position matches reality by checking: (a) pose marker on map matches physical location, (b) /registered_scan point cloud aligns with map features. A "localization confidence" metric from FASTLIO2's iEKF innovation can be displayed as a percentage or color indicator. | `rosnav_docker.py:312-313` (registered_scan subscription), standard SLAM verification practice |
| 36 | What happens if the user sends a navigation goal while the robot is already navigating to a different goal? | The new goal replaces the old one. ROSNav's `set_goal()` cancels the previous navigation thread (`stop_navigation()` + `join(timeout=1.0)`) before starting a new one. This is explicit in the code. | `rosnav_docker.py:531-553` (set_goal cancels previous) |
| 37 | What happens if the user power-cycles or force-reboots the robot mid-navigation? | FASTLIO2 (Phase 1) starts from (0,0,0) on every boot -- no map corruption possible since there is no persistent map in Phase 1. The VoxelGridMapper rebuilds from live data. Docker containers are stateless by default. eMMC filesystem is journaled (ext4), so no corruption from power loss. | FASTLIO2 design (single-session), Docker stateless containers, standard Linux filesystem journaling |
| 38 | What happens if someone manually pushes or relocates the robot while SLAM is running? | FASTLIO2's tight IMU coupling handles small pushes (the iEKF fuses IMU + lidar to correct for unexpected motion). Large displacement (>1-2m) without corresponding IMU data will cause localization divergence. The system should detect this via scan-to-map matching quality and warn the operator. This is inherent to all LIO SLAM systems. | FASTLIO2 iEKF architecture, standard LIO behavior |
| 39 | Can a user who does not know ROS, Docker, or Python operate this system day-to-day? | Yes, that is the target. Daily operation should be: open web UI, verify ready, tap destination, monitor. No SSH/terminal needed for normal operation. deploy.sh handles deployment. The command center web UI at port 7779 is the operator interface. Recovery actions (container restart) should be exposed as UI buttons. | `launch_nos.py:107` (Web UI URL), command center design intent |
| 41 | Is there an audible or physical indicator when the robot changes state? | The M20 has front and back flashlights controllable via the inspection protocol (Type 1101, Command 2). These can be programmatically toggled. The M20 also has a hardware emergency stop button with status feedback. No built-in speaker, but LEDs can signal state changes. Audible alerts would require external hardware. | M20 dev guide Section 1.2.6 (Flashlight control), Section 1.3.1.1 (HES status) |
| 43 | How should the system communicate the difference between host process health and nav container health? | Two-tier health indicator: (a) "System" = host dimos process, (b) "Navigation" = Docker container + SLAM. If the host is down, nothing works (no UI). If the container is down, the UI remains accessible showing "Navigation: Offline" with a restart button. DockerModule status is queryable via docker inspect. | `docker_runner.py:127-132` (_is_container_running), `docker_runner.py:77` (restart policy) |
| 44 | Should lidar health (dual RSAIRY merge status, partial failure) be a persistent indicator or only surfaced on failure? | Persistent but subtle. A small icon showing "2/2 lidars" in the status bar, turning to "1/2" (yellow warning) on partial failure. rsdriver merges both lidars with `send_separately: false`, so a single lidar failure still produces a merged cloud but with a blind spot. The entrypoint already has lidar health recovery logic. | `entrypoint.sh:70-97` (lidar health check), rsdriver config (send_separately: false) |
| 47 | How does the operator trigger a container restart or system recovery without SSH access? | Expose a "Restart Navigation" button in the web UI that calls the host-side dimos API, which does `docker restart <container_name>`. The DockerModule class already has stop/start lifecycle methods. This should be a deliberate action (confirmation dialog) not a casual button. | `docker_runner.py:159-198` (DockerModule lifecycle) |
| 48 | What feedback does the operator receive between sending a goal and the robot's first movement? | The UI should show: (a) "Goal received" instantly when the goal is published, (b) "Planning..." while FAR planner computes path, (c) "Navigating" when first cmd_vel is received. ROSNav subscribes to /path -- once a path is published, navigation has begun. The latency is typically <1s for FAR planner. | `rosnav_docker.py:320` (path subscription), `rosnav_docker.py:374-377` (path forwarding) |
| 49 | What is the interaction for switching between teleop and autonomous mode, and how is dual-control prevented? | The M20 has explicit mode switching via the inspection protocol (Type 1101, Command 5): Regular Mode=0 (teleop/UDP), Navigation Mode=1 (/NAV_CMD). ROSNav sets autonomy via Joy message (button 7). Only one mode is active at a time -- the firmware enforces this. The UI should show current mode clearly and require explicit toggle. | M20 dev guide Section 1.2.2 (Usage Mode), `rosnav_docker.py:403-429` (_set_autonomy_mode via Joy) |
| 50 | What is the boot sequence from the operator's perspective? | 1. Power on (hardware boot ~30s). 2. "Connecting to robot..." (UDP heartbeat establishing ~2s). 3. "Waiting for sensors..." (/ODOM + /IMU wait, up to 30s). 4. "Checking lidar..." (15s timeout). 5. "Starting navigation..." (Docker container + FASTLIO2 init ~10s). 6. "Ready." Total: ~60-90s. Each phase should be a named step in a progress indicator. | `entrypoint.sh:34-98` (sequential boot checks), `docker_runner.py:97` (120s startup timeout) |
| 51 | Error flow: NOS runs out of RAM. What does the operator see before the OOM killer strikes? | Currently nothing -- OOM killer is silent. The system should monitor /proc/meminfo and warn at 80% usage ("Memory critical -- consider stopping non-essential modules"). NOS has 4GB swap configured which provides a buffer. Docker container has no explicit memory limit (should add `--memory` flag). The sshd OOM protection prevents losing SSH access. | Memory context: NOS ~2GB usable RAM + 4GB swap, `docker_runner.py` (no memory limit flag currently) |
| 55 | Where should the emergency stop / cancel goal button be placed? | Always visible, bottom-right corner (thumb-reachable on mobile), red, large touch target (min 48x48dp). Must be present on every screen state. The M20's hardware HES is a separate physical button on the robot body. The software cancel should be equally discoverable in the UI. | Industry standard (ISO 13849, IEC 62061 for software e-stop placement), universal UX |
| 56 | How should the UI show transition states like "starting," "becoming ready," or "recovering" versus a hard failure? | Transition states: pulsing/animated indicator with estimated time. Hard failure: solid red with error description and action button. Time threshold: if a transition state exceeds 2x expected duration, escalate to warning. If it exceeds 5x, show as failure with manual recovery option. | Industry UX best practice for state machines |
| 57 | How should the Docker container auto-restart be communicated? | Toast notification: "Navigation restarting (attempt 2/3)..." with a progress spinner. If all 3 attempts fail: persistent error banner "Navigation failed -- manual restart required" with a restart button. The restart policy "on-failure:3" means 3 attempts. Show restart count to help diagnose persistent crashes. | `docker_runner.py:77` (on-failure:3 policy) |
| 62 | What should happen during peak pedestrian or forklift traffic periods? | The FAR planner + base_autonomy handles dynamic obstacles via reactive avoidance. The system should reduce speed in congested areas (configurable speed zones). If path is consistently blocked, escalate to "Path blocked -- waiting for clearance" after N seconds, then suggest alternative route. This is standard AMR behavior. | FAR planner + base_autonomy design (reactive obstacle avoidance), industry AMR practice |
| 63 | How should the robot recover after emergency stops, fire drills, or manual shutdowns mid-mission? | After e-stop release or reboot: system returns to IDLE state. Previous mission goal is NOT auto-resumed (safety requirement -- operator must explicitly re-dispatch). FASTLIO2 re-converges from current position. The /NAV_CMD topic requires explicit re-engagement (Navigation Mode must be re-set). | M20 dev guide: motion state transitions require explicit commands, `rosnav_docker.py:524-528` (stop clears state) |
| 68 | Is the real problem that lio_perception cannot relocalize, or that the monolithic architecture cannot be iterated on quickly? | Both, but they are separable. The architecture problem (everything in one Docker container, no host dimos) blocks rapid iteration. The relocalization problem (lio_perception has no loop closure) blocks multi-floor. Phase 1 solves architecture, Phase 2 solves relocalization. The plan correctly separates these. They should not be conflated in success criteria. | Plan Phases 1 vs 2, context.md data flow |
| 70 | Is replacing the entire perception-to-navigation pipeline simultaneously the right approach, or should components be replaced incrementally? | The ROSNav pattern is inherently incremental. Phase 1 replaces the architecture and SLAM (lio_perception -> FASTLIO2) but keeps the same data flow. Phase 2 swaps SLAM implementation (FASTLIO2 -> arise_slam) inside the container. Phase 3 adds multi-floor logic. The container boundary enables swapping components without touching the host. | Plan phased approach, ROSNav container isolation |
| 71 | Will battery consumption change with the new architecture? | Likely minimal increase. Host Python process adds ~100-200MB RAM overhead. Docker container overhead is negligible (shared kernel). FASTLIO2 is lighter than lio_perception (iEKF vs full SLAM). FAR planner runs in the container. Net compute may be comparable. The M20's battery status is reported at 2Hz via inspection protocol (BatteryLevelLeft/Right). | M20 dev guide Section 1.3.1.3 (battery status), FASTLIO2 lightweight design |
| 72 | Will the robot be louder due to increased compute load? | Unlikely. The RK3588 SoC on NOS is passively cooled (no fan). AOS and GOS are unchanged. The M20's noise comes from servo motors and gaits, not compute. If anything, moving SLAM from AOS to the container on NOS redistributes load without increasing total. | NOS hardware (RK3588 passive cooling), M20 physical design |
| 73 | Can the user still view individual lidar feeds to diagnose physical damage? | Not directly in the current architecture. rsdriver merges both lidars before publishing to /lidar_points. Individual feeds would require subscribing to the raw multicast groups (224.10.10.201:6691, 224.10.10.202:6692) separately. This is a diagnostic feature that could be added as a debug mode but is not needed for daily operation. | rsdriver config (send_separately: false), multicast addresses from plan |
| 74 | Does the new system handle dynamic obstacles as well or better? | Better. The current A* planner is a global planner with limited reactive avoidance. The CMU stack includes base_autonomy which has dedicated local obstacle avoidance. FAR planner handles both known and unknown environments. The combination provides better dynamic obstacle handling than replanning A*. | Context.md: FAR planner + base_autonomy, current replanning_a_star limitations |
| 75 | Will the system work the same on all M20 units, or does each need individual calibration? | Same deployment works across M20 units. The M20 hardware is standardized (same lidar placement, same RSAIRY sensors, same board layout). Robot-specific parameters are: IP addresses (already configurable), lidar_height (0.47, same across units), robot_width (0.45, same). No per-robot calibration needed for the nav stack. | `launch_nos.py:80` (lidar_height=0.47), M20 standardized hardware |
| 76 | Is the goal-setting interface changing at all? | The mechanism changes but the user-facing interaction should not. Currently goals are set via the web UI command center. With ROSNav, goals are published to /goal_pose (ROS) instead of directly to the A* planner. The web UI remains the same -- user taps a location on the map. The backend routing is transparent. | `rosnav_docker.py:257` (goal_req: In[PoseStamped]), same interface contract |
| 78 | How does the user build or load a map for the first time, and does FASTLIO2 require different mapping procedures? | FASTLIO2 mapping is simpler than lio_perception: drive the robot around and FASTLIO2 builds the map in real-time. No special driving patterns or loop closure procedures needed (Phase 1 has no loop closure). The user drives via teleop while FASTLIO2 builds the map from /lidar_points + /IMU. Map is the accumulated registered scans. | FASTLIO2 design (real-time map building from lidar+IMU) |
| 79 | What does the user do at end of shift -- must they shut down gracefully, or can they just power off? | Can just power off. Phase 1 has no persistent maps to corrupt (FASTLIO2 is single-session). Docker containers are stateless. The M20 firmware handles abrupt power loss gracefully. The sshd OOM protection ensures the system does not hang. The only downside: next boot starts fresh (no map persistence until Phase 2). | FASTLIO2 single-session design, Docker statelessness, NOS swap/OOM hardening |
| 82 | What happens if the user starts the system in a featureless area? | FASTLIO2's iEKF will have high innovation/residuals due to lack of geometric features for scan matching. The system should detect degenerate scans (low feature count or high condition number) and warn: "Low feature environment -- localization may be unreliable. Move to area with more structure." This is a known SLAM challenge. | FASTLIO2 iEKF degeneracy detection (standard feature), industry SLAM practice |
| 83 | What happens if the user deploys in an environment with glass walls, mirrors, or highly reflective surfaces? | Lidar passes through glass and reflects off mirrors, creating phantom obstacles or missing walls. RoboSense RSAIRY lidars are mechanical 360-degree scanners and share this limitation with all lidar-based systems. The costmap will show phantom objects. Mitigation: map editing to mark known glass areas as obstacles or free space. | Universal lidar limitation, RoboSense RSAIRY mechanical design |
| 84 | What happens if a user sends the robot to a floor that has not been mapped yet? | Phase 1 has no multi-floor concept -- single-session FASTLIO2 only knows the current floor. "Floor" is not a concept until Phase 2. If multi-floor is exposed in Phase 2+, the system should return a clear error: "Floor 3 has no map. Map this floor first." rather than silently failing. | Plan Phase 1 (single-floor only), Phase 2 (arise_slam adds floor awareness) |
| 85 | What if WiFi between NOS and the user's monitoring device drops during a mission? | The robot continues navigating autonomously. The web UI reconnects when WiFi returns. No mission state is lost. The nav stack runs entirely on NOS (host + container). WiFi is only for the monitoring web UI connection. This is by design -- the robot operates independently of the monitoring device. | Architecture: nav stack runs on NOS, web UI is monitoring-only |
| 87 | Can a colorblind user interpret the web visualization? | The current costmap uses the "Accent" colormap with opacity 0.2. Status indicators should follow WCAG 2.1 guidelines: use shape + color (not color alone) for state distinction. The costmap should offer a colorblind-safe palette option. This is a design implementation detail, not an architecture question. | `launch_nos.py:62` (Accent colormap), WCAG 2.1 accessibility standard |
| 88 | Are log messages and diagnostics written in plain language? | Currently no -- they are developer-level ("drdds available -- /NAV_CMD publisher enabled"). For the pilot, user-facing status messages should be translated to plain language. Internal logs remain technical for debugging. The web UI should show user-friendly messages while logging detailed info to files. | `entrypoint.sh:45-46` (developer-level messages), best practice separation of operator vs developer messaging |
| 90 | What assumptions are we making about network infrastructure? | Assumed: (a) NOS-AOS-GOS communicate over wired Ethernet (10.21.31.x subnet, confirmed), (b) multicast works on the internal network (required for DDS + rsdriver), (c) WiFi access point on AOS wlan0 (10.21.41.1) for external access. NOT assumed: internet access during operation (only needed for Docker pulls). | `deploy.sh:39-41` (network modes), rsdriver multicast config |
| 91 | What happens for users in environments with strict IT security policies? | The deploy.sh uses GHCR (internet) for image pulls. Air-gapped deployment: pre-pull image, `docker save/load` via USB. No outbound internet needed during operation. All DDS traffic is local (internal robot network). The system does not phone home. | `deploy.sh:65` (GHCR_IMAGE), Docker save/load standard workflow |
| 92 | How should the system present the distinction between FASTLIO2 SLAM pose and the robot's raw odometry? | Show only one authoritative pose to operators (the SLAM pose, which is the fused estimate). Raw odometry (from /ODOM via lio_perception or dead reckoning) is internal. For debugging, a developer mode can show both with "SLAM" and "ODO" labels. Divergence between them indicates localization issues. | Standard robotics practice: expose fused estimate, hide internals |
| 93 | Should Phase 2 multi-floor relocalization expose "current floor" as a first-class concept in the UI? | Yes. "Current floor" should be a top-level indicator from Phase 2 onward. The UI information model should reserve space for it from Phase 1 (even if hidden/disabled). This prevents a UI redesign when Phase 2 ships. | Plan Phase 2/3 (multi-floor is core goal), forward-compatible UI design |
| 94 | How should container lifecycle state be presented relative to mission state? | Container state is infrastructure -- show it as a secondary indicator (small icon in status bar). Mission state is primary (large, central). If container is unhealthy, mission state automatically becomes "Navigation Offline." The user cares about "can I navigate?" not "is the container running?" Container details go in a diagnostic panel. | `docker_runner.py:127-132` (container status check), UX best practice (hide infrastructure) |
| 96 | Should velocity commands be shown as raw numbers, visual vectors, or abstracted? | Default: visual vector arrow on the robot icon showing direction + relative speed (short arrow = slow, long = fast). Developer mode: raw m/s values. The M20 max speed varies by gait (agile flat gait is fastest). Abstracted "fast/slow/stopped" loses too much info for debugging. | M20 dev guide gait speeds, UX best practice for motion visualization |
| 97 | How should the system communicate "degraded mode"? | Yellow warning banner with specific degradation: "Navigation active -- costmap stale (last update 30s ago)" or "Navigation active -- 1/2 lidars offline." System continues operating but user is informed. Each degradation should be independently indicated. Do not collapse multiple degradations into a single "degraded" state. | Industry AMR practice, modular health reporting |
| 98 | What should the "no map yet" state look like when FASTLIO2 is building its first map? | Show a progressively building point cloud visualization with a message: "Building map -- drive the robot to explore the area." The map grows in real-time as /registered_scan data arrives. This is the default first-time experience. A progress indicator is not possible (no way to know total area), so show coverage area instead. | FASTLIO2 real-time mapping, `rosnav_docker.py:368-369` (registered_scan to pointcloud) |
| 100 | What inputs are required vs. optional when setting a navigation goal? | Required: position (x, y in map frame). Optional with defaults: orientation (default: face direction of travel), speed_limit (default: max_linear_speed from config), frame_id (default: "map"). The ROSNav navigate_to() takes PoseStamped which includes position + orientation. For map-tap goals, orientation can be inferred from approach direction. | `rosnav_docker.py:466-504` (navigate_to signature), `launch_nos.py:73` (max_linear_speed=1.0) |
| 105 | Error flow: lidar topic goes stale. What happens in the UI? | The entrypoint has lidar health recovery: if /ALIGNED_POINTS has no data, it auto-restarts rsdriver (GOS) and lio_perception (AOS). The UI should show "Lidar: reconnecting..." during recovery. If recovery fails after the retry, show "Lidar offline -- contact support." FASTLIO2 stops producing valid poses without lidar input. | `entrypoint.sh:70-97` (lidar health check + auto-recovery) |
| 107 | What visual pattern should indicate planned path versus actual traveled path? | Planned path: solid blue line with direction arrows. Traveled path: dotted/dashed green line (fading with time). This is the standard convention in navigation UIs (Google Maps, RViz, Foxglove). Both should be shown simultaneously for operators to assess planning quality. | RViz/Foxglove conventions, standard nav UI practice |
| 110 | How does the system represent "waiting for lidar" versus "lidar healthy but no SLAM pose yet"? | "Waiting for lidar": boot phase, entrypoint is checking /ALIGNED_POINTS (show "Sensors: initializing..." with lidar icon greyed out). "Lidar healthy, no SLAM pose": FASTLIO2 starting up, needs a few scans to converge (show "Localizing..." with lidar icon green but position icon yellow). Two distinct boot stages with different icons. | `entrypoint.sh:70-97` (lidar check phase), FASTLIO2 convergence behavior |
| 111 | What state represents "robot is physically stuck but system is healthy"? | The replanning logic detects no progress (position unchanged despite cmd_vel output). State should be "STUCK" (distinct from NAVIGATING): "Robot appears stuck -- check for physical obstacles." After N seconds of no progress, auto-cancel navigation and alert operator. The NavigationState enum has RECOVERY which could cover this. | `base.py:25` (RECOVERY state), standard AMR stuck detection |
| 112 | What state represents "goal unreachable"? | The A* planner / FAR planner returns failure when no path exists. This should be a distinct state: "Goal unreachable -- no valid path found." Offer: (a) choose different goal, (b) show why (costmap overlay showing blocked area), (c) retry after obstacle clears. Different from "path blocked" (path exists but is temporarily obstructed). | Standard path planning failure handling |
| 113 | What does the "shutdown" flow look like? | Operator presses "Shutdown" in UI. System: (a) stops navigation (cancel goal), (b) sends zero velocity, (c) stops Docker container (DockerModule.stop() -> docker stop with 30s grace), (d) stops host dimos. The operator can also just power off (safe in Phase 1). Graceful shutdown preserves logs. | `rosnav_docker.py:592-608` (stop sequence), `docker_runner.py:44` (30s grace period) |
| 114 | How does the system handle the transition when NOS loses network connectivity to AOS? | DDS topics from AOS (/ODOM, /ALIGNED_POINTS, /IMU) stop flowing. FASTLIO2 loses sensor input and stops producing poses. The system should detect topic timeout and transition to safe state: zero velocity + "Sensor connection lost -- check network." The existing entrypoint topic wait logic has a 30s timeout model. | DDS topic dependency, `entrypoint.sh:34-42` (topic timeout model) |
| 115 | What is the operational definition of "map"? | Phase 1: Live SLAM map only. FASTLIO2 builds a map in real-time from lidar+IMU. No persistent/pre-built maps. Every session starts fresh. Phase 2 (arise_slam): Hybrid -- pre-built reference map for relocalization + live updates. "Loading a map" = loading a saved arise_slam pose graph for a specific floor. | Plan Phase 1 (FASTLIO2 single-session), Phase 2 (arise_slam map persistence) |
| 116 | What is "base autonomy" in the CMU nav stack versus what dimos provides? | base_autonomy (CMU): local obstacle avoidance, terrain analysis, speed adaptation, reactive safety stops. Runs inside the Docker container. dimos: module coordination, UI/visualization, robot connection, goal management, fleet-level logic. The boundary is clear: CMU stack handles low-level navigation, dimos handles high-level orchestration and UX. | Context.md Section 8, ROSNav bridge pattern |
| 117 | What is the relationship between the container-side FAR planner costmap and the host-side dimos CostMapper? | In the ROSNav architecture, the FAR planner has its own internal costmap for route planning. The host-side CostMapper produces a costmap for visualization and the dimos web UI. They may diverge. The FAR planner's costmap is authoritative for navigation decisions. The host-side costmap is for display only. This matches the G1 reference implementation. | Context.md data flow (target pipeline), `launch_nos.py:68-71` (host CostMapper for viz) |
| 118 | How do the M20's proprietary drdds and standard ROS2 DDS interoperate? | drdds publishes NavCmd on /NAV_CMD as a standard DDS topic (not ROS2-wrapped). M20ROSSensors uses rclpy to subscribe to standard ROS2 topics (/ODOM, /IMU, /ALIGNED_POINTS) which are published by lio_perception via FastRTPS. The drdds .msg files are rebuilt for Python 3.10 during Docker build. They share the same DDS domain (ROS_DOMAIN_ID=0 on robot). | `entrypoint.sh:12-13` (FastRTPS + domain 0), `ros_sensors.py` (rclpy subscriptions), `entrypoint.sh:18-19` (drdds PYTHONPATH) |
| 119 | What if a separate ROS2 application is running on the same network -- could DDS discovery cause topic collisions? | Yes. DDS uses multicast discovery on the same domain. The nav container uses ROS_DOMAIN_ID=42 to isolate from the robot's Foxy stack (domain 0). However, if another Humble application uses domain 42, collisions occur. Mitigation: unique ROS_DOMAIN_ID per robot, documented in deployment config. | `rosnav_docker.py:121` (ROS_DOMAIN_ID=42), standard DDS isolation |
| 120 | What if the Docker container runs out of memory on NOS? | Docker container has no explicit memory limit. On NOS with ~2GB usable + 4GB swap, the OOM killer will target the container (highest memory consumer). The container should set `--memory=1.5g` to fail predictably before OOM-killing the host. DockerModuleConfig should add a `docker_memory` field. | `docker_runner.py:49-101` (no memory limit field), NOS RAM constraints |
| 121 | What if the user accidentally triggers navigation while the robot is on a charging dock? | The M20 reports charging status via inspection protocol (Charge field in BasicStatus). The system should check: if Charge != 0 (idle), warn "Robot is charging -- undock before navigating" and reject the goal. The robot must physically exit the dock before navigation. | M20 dev guide Section 1.3.1.1 (Charge status: 0=Idle, 1=Enter dock, 2=Charging, 3=Exiting) |
| 122 | What if the user wants to add a new floor after initial deployment? | Phase 1: Not applicable (single-session, no persistent maps). Phase 2+: arise_slam supports incremental mapping -- drive the new floor to build its pose graph, save it as a new floor layer. The multi-floor architecture should support adding floors without invalidating existing ones. | Plan Phase 2 (arise_slam map management), standard multi-floor mapping |
| 127 | How should the system ask for missing information without feeling blocking? | Non-blocking prompts with sensible defaults. Example: if NOS SSH is unreachable, show "Cannot reach navigation board -- check connection" as a warning banner, not a modal. The system should degrade gracefully (e.g., show cached data, disable nav features) rather than blocking the entire UI. | UX best practice for progressive disclosure |
| 129 | What feedback should appear after success? | When goal is reached: toast notification "Arrived at [destination name]" + map zooms to arrival location + path visualization shows completed. Auto-dismiss after 5s. If next waypoint exists (patrol route), show "Proceeding to next waypoint..." The robot returns to IDLE and awaits next command. | Standard navigation completion UX, `rosnav_docker.py:349-354` (goal_reached callback) |
| 130 | Should the interface show deployment-stage context like "Phase 1 pilot" versus "full multi-floor mode"? | Yes, subtly. A small label in the footer or settings panel: "Navigation: Single-Floor Mode (Phase 1)". This sets expectations and prevents users from attempting multi-floor operations. When Phase 2 ships, it changes to "Multi-Floor Mode." Not prominent -- just discoverable. | Forward-compatible UX, expectation management |
| 133 | How should the system request the SSH sudo password if manual NOS intervention is required through the UI? | It should not. The UI should never expose SSH/sudo to operators. All recovery actions should be wrapped in safe API calls (restart container, restart lidar pipeline). If SSH-level intervention is truly needed, that is a developer task, not an operator task. The NOS sudo password (`'`) is a security hazard to expose in UI. | Security best practice, operator vs developer role separation |
| 134 | How should the system visually group mapping modules versus navigation modules? | In the diagnostic/developer panel: group by function. "Mapping" section: VoxelGridMapper, CostMapper status. "Navigation" section: FAR planner, base_autonomy, FASTLIO2 status. "Robot" section: M20Connection, sensors. This is a developer view only -- operators see the unified state machine. | Module architecture from context.md, UX role separation |
| 136 | What interactions are available when viewing the 3D VoxelBlockGrid map? | Pan (click+drag), zoom (scroll), rotate (right-click+drag), filter by height (slider). These are standard 3D visualization controls, already supported by Rerun's web viewer. The VoxelGridMapper publishes to RerunBridge which renders as 3D boxes. | `launch_nos.py:57-58` (to_rerun with mode="boxes"), Rerun viewer built-in interactions |
| 137 | How should system resource usage be displayed? | Under a "Diagnostics" tab, not front-and-center. Show: CPU %, RAM usage (with NOS 2GB context), disk usage, container status. Only surface on the main screen if thresholds are exceeded (>80% RAM = yellow warning). Operators do not need resource monitoring for normal operation. | UX best practice (progressive disclosure) |
| 138 | How should active RPC calls between host and container be visualized, if at all? | Not shown to operators. RPC is infrastructure. For developers, a "System" debug panel could show LCM topic activity (publish/subscribe rates). The LCMRPC layer is transparent to users. Only surface RPC failures as "Communication error" in the status bar. | `docker_runner.py:188-189` (RPC setup), infrastructure-level detail |
| 139 | How should secondary telemetry (like the 512MB rerun memory limit) be displayed? | Not displayed to operators. This is a developer configuration detail. If Rerun hits its memory limit, it drops old history silently (by design). The user sees current state, not history. Developer diagnostics panel can show Rerun memory usage for troubleshooting. | `launch_nos.py:83` (memory_limit="512MB"), Rerun design |
| 140 | What color coding should separate host-level processes from container-level processes? | In the developer diagnostics panel only: host processes in blue, container processes in green. This distinction is irrelevant to operators. The unified state machine abstracts this boundary. If exposed, keep it consistent and documented in the developer panel legend. | UX role separation |
| 141 | What iconography best represents dual lidar merging status? | Two overlapping fan/cone icons representing front and back lidars, merging into a single circle. Green = both healthy. Yellow with "1/2" = one failed. Red = both failed. The merged output (/lidar_points) is what matters operationally. | rsdriver merge behavior, standard sensor health iconography |
| 142 | Should the robot's current pose be strictly centered on screen, | Default: robot centered with auto-follow. User can pan freely (breaks auto-follow). A "re-center" button (crosshair icon) restores auto-follow. This is the standard pattern (Google Maps, RViz, every navigation app). | Universal navigation UI pattern |
| 143 | How should height cost (terrain slope gradient) be displayed on the costmap? | As a color gradient overlay on the 2D map: green (low cost/flat) through yellow (moderate slope) to red (lethal/untraversable). The current implementation uses "Accent" colormap with opacity 0.2. Height cost values 0-100 map to the gradient. Only shown when costmap layer is enabled. | `launch_nos.py:61-62` (Accent colormap, opacity 0.2), `costmapper.py` (height_cost 0-100) |
| 144 | How does the system handle clock drift between the three boards and the Docker container? | ROS2 DDS messages carry timestamps from the publishing board. Clock drift between AOS/GOS/NOS can cause TF tree inconsistencies. Mitigation: NTP synchronization between boards (standard practice). The Docker container shares NOS's clock (--network host shares the time namespace). Drift >100ms would cause visible scan misalignment. | Standard multi-board robotics practice, Docker host networking (shared clock) |
| 145 | How does the system handle electromagnetic interference in a industrial manufacturing facility? | EMI can affect WiFi and potentially GPS but should not affect wired Ethernet (10.21.31.x network) or lidar (optical). The M20's internal communication uses wired Ethernet between boards. The lidar uses multicast on the internal network. Only the operator's WiFi connection to AOS wlan0 is vulnerable to EMI. | M20 wired internal network, lidar optical sensing (immune to EMI) |
| 146 | What happens when two M20 robots operate on the same floor -- can their lidar scans interfere? | RoboSense RSAIRY lidars use 905nm laser. Multi-robot lidar cross-talk is theoretically possible but extremely rare in practice (the probability of two beams hitting the same point at the same time is negligible). DDS topic isolation is the real concern -- each robot needs a unique ROS_DOMAIN_ID to prevent topic collisions. | RoboSense RSAIRY specs, DDS domain isolation |
| 147 | What if the payload characteristics change significantly between floors? | The M20 reports payload via MotionStatus (though currently marked "Invalid parameter" in the dev guide). The robot's gait controller handles payload changes dynamically. Speed limits in the velocity controller cap maximum velocities regardless of payload. The nav stack does not need payload-aware planning for the pilot. | M20 dev guide Section 1.3.1.2 (Payload field, currently invalid), velocity limiting in VelocityController |
| 99 | How should goal/waypoint information be structured? | For the factory pilot: ordered queue (patrol route = sequence of waypoints). The UI should show: current waypoint (highlighted), upcoming waypoints (numbered list), completed waypoints (checked off). ROSNav's navigate_to() handles one goal at a time; the queue is managed by the host-side coordinator (WavefrontExplorer or a patrol module). | `launch_nos.py:79` (wavefront_frontier_explorer), `rosnav_docker.py:531-553` (single goal with queue management) |
| 101 | How does the operator mark an area as "do not enter" or adjust the costmap? | The CostMapper produces an OccupancyGrid from height cost analysis. Real-time "no-go zones" can be implemented by overlaying operator-defined polygons as lethal cost (100) on the costmap. This is a standard feature in AMR systems. The UI needs a polygon drawing tool on the map view. Implementation: host-side costmap overlay before publishing to the planner. | `costmapper.py` (OccupancyGrid output), standard AMR virtual barrier feature |
| 104 | What is the flow for the initial SLAM map building session? | 1. Start system in new area. 2. Switch to teleop mode. 3. Drive robot through the space (FASTLIO2 builds map in real-time). 4. Web UI shows growing point cloud. 5. When coverage is sufficient, operator saves map (Phase 2) or begins autonomous operation (Phase 1 -- map exists only in memory). No loop closure needed in Phase 1 for single-floor. | FASTLIO2 real-time mapping, Phase 1 (no persistence) |
| 106 | What happens when the robot reaches a goal? | ROSNav publishes goal_reached (Bool=true). NavigationState transitions to IDLE. The robot stops moving (soft_stop_pub sends Int8=2). No auto-return or auto-proceed. The operator or patrol logic decides next action. For patrol routes, the coordinator sends the next waypoint. | `rosnav_docker.py:349-354` (goal_reached -> IDLE), `rosnav_docker.py:496-498` (soft stop on completion) |
| 108 | Should the interface support dark mode? | Yes, it should default to dark mode. Factory environments vary in lighting, and the web UI (Rerun viewer + command center) is typically viewed on screens in mixed lighting. Dark mode reduces eye strain and improves contrast for map visualization. Rerun's web viewer already supports dark themes. | Rerun viewer dark theme support, industry practice for monitoring UIs |
| 109 | What is the appropriate map zoom behavior? | Default: auto-zoom to show robot + immediate surroundings (10m radius). When navigating: auto-zoom to show robot + goal + path. On goal set: briefly zoom to show full route, then return to navigation zoom. Manual zoom overrides auto-zoom until "re-center" is pressed. | Standard navigation UI zoom behavior |
| 123 | Can a user with limited mobility operate the teleop fallback controls? | Teleop uses gamepad-style controls (axis commands X, Y, Yaw normalized to [-1,1]). The web UI should offer keyboard controls as an alternative (arrow keys for direction, +/- for speed). The M20 dev guide recommends 20Hz control frequency. Accessibility accommodations would need custom input mapping (assistive device support is not in scope for pilot). | M20 dev guide Section 1.2.5 (axis commands at 20Hz) |
| 124 | What if the operator's monitoring device has a small screen (phone) versus a large screen (tablet)? | The web UI (command center) should be responsive. Mobile: simplified view (map + status + e-stop). Tablet/desktop: full view (map + status + costmap layers + diagnostics). The Rerun web viewer is already responsive. The command center at port 7779 needs responsive CSS. | Web UI (port 7779), standard responsive web design |
| 125 | Are we assuming the user has physical access to the NOS board? | No. NOS is inside the robot body. Physical access requires opening the robot. All user interaction is via web UI (WiFi) or the M20's physical buttons (power, HES). SSH access to NOS is developer-only (via ProxyJump through AOS WiFi). | M20 physical design, `deploy.sh:39` (SSH via ProxyJump) |
| 126 | Are we assuming a specific level of familiarity with M20 hardware basics? | For operators: basic familiarity (power on/off, HES button location, charging dock). No knowledge of boards, networking, or software architecture. For deployment/setup: deployer knows SSH, Docker basics, network configuration. The pilot should include a quick-start card for operators. | Operator vs deployer role separation |
| 131 | Do users need ownership context such as "who started this run"? | Not for single-robot pilot. With one robot and likely one operator per shift, ownership is implicit. For fleet operations (future), yes. For now, log the operator's session start time and any goal commands with timestamps. This is sufficient audit trail for the pilot. | Single-robot pilot scope |
| 132 | What audit or confirmation trail is needed when key state transitions happen? | Log all state transitions with timestamps to persistent storage: goal set, navigation started, goal reached, errors, recoveries, e-stops. Accessible via "History" tab in diagnostic panel. For the pilot, a simple timestamped event log is sufficient. Formal audit trails are a post-pilot feature. | Pilot scope, standard event logging |
| 135 | In a service conflict, what info does the user need to resolve it? | The user should not need to resolve service conflicts. deploy.sh already handles them: disables height_map_nav (AOS), planner (NOS), dimos-mac-bridge (NOS) automatically. If a new conflict arises, the system should detect it (e.g., two publishers on /NAV_CMD) and report "Navigation conflict detected -- contact support." | `deploy.sh:281-299` (check_conflicts auto-disables) |

---

## Branch Points (Human Decision Required)

| # | Question | Why Human Needed |
|---|----------|------------------|
| 2 | What does "multi-floor transition" actually mean in the pilot factory -- elevator, ramp, manual carry, or a combination? | Site-specific physical infrastructure. Determines whether this is discrete map-switching or continuous 3D traversal. No way to infer from codebase. |
| 4 | Will users expect noticeably better navigation from day one, and does Phase 1 deliver user-visible value? | Depends on what "better" means to pilot stakeholders. Phase 1 swaps architecture + SLAM but may not visibly improve navigation quality. Business expectation question. |
| 5 | Will multi-floor elevator transitions happen automatically, or does the user need to intervene at each floor? | Depends on elevator infrastructure (API-accessible or manual button press). pilot facility-specific. Determines Phase 3 scope significantly. |
| 8 | Will existing saved maps, waypoints, and mission definitions carry over? | Current system uses lio_perception maps on AOS (/var/opt/robot/data/maps/active). FASTLIO2 uses a different format. Technical migration is possible but costly. Business decision on whether the effort is justified for pilot. |
| 9 | Who is the primary end-user during the factory pilot -- robotics engineer, factory floor technician, or site manager? | Business/deployment decision. Fundamentally changes UI complexity, terminology, error message style, and training requirements. |
| 10 | What job is the robot actually doing during the pilot -- patrol, inspection, delivery, or cross-floor handoff? | Business scope question. Determines success metrics, navigation patterns, post-arrival behavior, and integration points. |
| 11 | What spaces are in scope and what spaces are explicitly out of bounds? | pilot factory-specific. Determines map coverage requirements, restricted areas, safety zones. Cannot be inferred. |
| 12 | What is explicitly NOT being solved in Phases 1-3? | Scope boundary decision. Elevator API integration, fleet management, dynamic obstacle prediction, load/unload automation -- which are promised, which are deferred? Only the team knows. |
| 13 | How do established factory AMR platforms handle multi-floor navigation, and what benchmark does the pilot customer expect? | Competitive analysis + customer expectation. the pilot customer may benchmark against MiR, OTTO, Geek+, HIK Vision. Their specific expectations shape our MVP. |
| 14 | What percentage of in-scope missions must complete without human intervention? | Business success criterion. 80%? 95%? 99%? Each level implies dramatically different engineering effort and timeline. |
| 15 | After a floor transition or manual relocation, how quickly must the robot become operational? | Performance SLA. Sub-5s is industry standard for AMRs. arise_slam relocalization time is unknown until Phase 2 testing. Business must set the bar. |
| 16 | How many operator interventions per shift are acceptable? | Operational tolerance. Determines how robust recovery behaviors must be. 1/shift is very different from 10/shift. |
| 18 | How should the system behave when the factory floor layout has changed significantly? | Policy question: stop and alert? Attempt to replan? Refuse to navigate? Each has different safety and operational implications. Multiple valid approaches. |
| 24 | How should multi-floor context be represented visually -- tabs, floor picker, 3D building view, or stacked cards? | Design preference with significant UX implications. Each metaphor has tradeoffs for speed vs. spatial understanding. Needs design iteration with user feedback. |
| 25 | What is the "factory pilot" in business terms -- demo, shadow run, or production-adjacent deployment? | Business classification. A demo has different rigor requirements than a shadow run alongside existing processes or a production deployment. |
| 26 | Is the real problem floor-to-floor operation, or that recovery currently needs expert intervention? | Strategic direction. If babysitting reduction is more valuable than multi-floor, Phase 1 priorities shift. Only business stakeholders can judge relative value. |
| 27 | What uptime across shifts/days/weeks is required? | SLA definition. Demos: hours. Shadow runs: days. Production: weeks of continuous operation. Determines testing and hardening investment. |
| 28 | What site-specific safety, compliance, or regulatory requirements govern autonomous movement in the pilot factory? | China-specific regulations (GB/T 30029, GB/T 20721) and the pilot customer internal safety policies. Cannot be inferred. Legal/compliance question. |
| 31 | Can users roll back to the old system if the new one has problems in the field? | Rollback strategy. Technically possible (keep old Docker image, revert deploy.sh), but requires planning. Business must decide if rollback capability is a requirement vs. nice-to-have. |
| 33 | What is the process for a new operator joining mid-pilot? | Training and knowledge transfer process. Depends on operator skill level (Q9), available training time, and whether documentation/video guides are in scope. |
| 34 | What does the user do when the robot gets stuck -- are recovery actions available without SSH? | Overlaps with Q47 (container restart via UI) but broader: what specific recovery actions are exposed? List of recovery buttons in UI is a product decision. |
| 40 | Are status messages and error reports available in Chinese? | Localization decision. The pilot customer is a Chinese company. If operators read only Chinese, this is a hard requirement. If there is an English-speaking liaison, it is optional. |
| 42 | What is the single most important piece of information at a glance -- mission status or system health? | Information hierarchy decision. Both are valid primary indicators. The choice affects entire UI layout. Depends on operator workflow (monitoring vs. dispatching). |
| 45 | What map layers should be visible by default? | UX default decision. Options: costmap only, costmap+path, costmap+path+pointcloud. Too many layers overwhelms; too few hides problems. Depends on operator skill (Q9). |
| 46 | How does an operator start autonomous navigation -- single "go" button, map tap, coordinate entry, or named-location picker? | Interaction model decision. Map tap is simplest but requires understanding of the map. Named locations require setup. Coordinate entry is expert-only. Depends on Q9 (user skill). |
| 52 | What is the flow for a shift handoff? | Operational process question. Options: robot stays on (continuous), robot reboots per shift, state summary screen for incoming operator. Depends on factory shift patterns. |
| 53 | Where does the navigation monitoring view live -- standalone web app, command center, Foxglove, or mobile? | Platform decision. Command center exists (port 7779). Foxglove is a separate tool. Mobile requires responsive design or native app. Depends on where operators actually are (desk? walking floor?). |
| 54 | Should the map view be 2D (top-down costmap), 3D (point cloud), or toggleable -- and what is the default? | View default decision. 2D is faster for goal-setting, 3D reveals terrain issues. Default depends on primary use case (dispatching vs. monitoring). |
| 58 | Which prior navigation failures were most painful for operators? | Historical field experience. Only people who have operated the M20 in the field can answer this. Drives priority of improvements. |
| 59 | What recovery workflow do operators already consider "normal"? | Field experience. If operators have accepted a manual step as routine, replacing it may not be the highest-value improvement. Need operator input. |
| 60 | How often do pilot missions actually require floor transitions? | the pilot customer operational data. If 95% of missions are single-floor, highly reliable single-floor autonomy may be sufficient for pilot success. Determines Phase 1 vs Phase 2 urgency. |
| 61 | Are we solving for fewer failed missions, fewer human touches, or faster site commissioning? | Primary success metric choice. Each optimizes different parts of the system. Business decision that shapes engineering priorities. |
| 64 | What environmental conditions have historically broken robots at this site? | Site-specific knowledge. Dust, steam, reflective surfaces, condensation, temperature extremes -- the pilot customer industrial manufacturing has specific hazards. |
| 65 | What promises have already been made to pilot stakeholders? | Hidden commitments. Scope conflict from pre-existing promises. Only the business team knows what has been communicated to the pilot customer. |
| 66 | What safety indicators must improve or at least not regress? | Safety baseline. Which safety metrics are tracked today? What does the customer's safety team require? Cannot be inferred from code. |
| 67 | What operator-confidence signals will count as success beyond hard metrics? | Qualitative success criteria. "Operators trust the robot" is subjective. How is it measured? Surveys? Observation? Interview? Business decision. |
| 69 | Does the three-phase plan address the risk that Phase 2 (arise_slam porting) may be research rather than engineering? | Risk assessment requiring domain expertise. Livox-to-RoboSense porting difficulty depends on scan pattern differences. Opus flagged this as potentially research-level. Need input from SLAM experts or the CMU team. |
| 77 | What does the user experience when they arrive at the factory site -- first-time setup flow? | Deployment UX. Options: deploy.sh from laptop, pre-configured USB image, factory-provisioned. First impression matters. Business/logistics decision. |
| 80 | How does the user update the system when a new version is available? | Update mechanism decision. Current: deploy.sh push/pull (requires SSH knowledge). Options: OTA from cloud, USB drive, single-button update in UI. Depends on operator skill (Q9) and network access (Q91). |
| 81 | What information does the user need to report a problem? | Support workflow design. Options: one-click log bundle, screenshot + description form, automatic telemetry. Depends on support team structure and capabilities. |
| 86 | What if multiple users send conflicting goals to the same robot? | Command authority model. Options: last-command-wins, first-command-locks, role-based priority. For single-robot pilot likely not critical, but policy matters for fleet. |
| 89 | Is there documentation or training material for non-technical factory staff? | Content creation decision. Quick-start card? Video tutorials? In-app guided tour? Depends on budget, timeline, and operator language (Q40). |
| 95 | What information density is appropriate for field engineer vs. remote developer? | Role-based view decision. Options: single view with toggle, two separate UIs, progressive disclosure. Depends on how many distinct roles interact with the system during pilot. |
| 102 | How should the operator provide feedback when the robot takes a bad path? | Feedback mechanism design. Options: "thumbs down" button, path correction drawing, verbal report to engineer. Depends on how feedback is used (training data? bug reports?). |
| 103 | How does an operator set up a repeatable patrol route? | Workflow design for a core the pilot customer use case. Options: drive-and-record, point-on-map sequence, import from file, voice command. Multiple valid UX approaches. |
| 128 | What level of permissions should different roles have? | RBAC design. Operator gets pause/resume/goal. Supervisor gets reconfigure/update/restart. Admin gets SSH. Depends on organizational structure at the pilot site. |

---

## Question Dependencies

### Cascading Chains

**Q9 (end-user identity) unlocks:**
- Q33 (onboarding process)
- Q39 (non-technical operation, partially auto-answered but depth depends on Q9)
- Q40 (language requirements)
- Q42 (information hierarchy)
- Q45 (default map layers)
- Q46 (goal-setting interaction)
- Q53 (monitoring platform)
- Q54 (2D vs 3D default)
- Q80 (update mechanism)
- Q89 (training materials)
- Q95 (information density)
- Q126 (hardware familiarity assumption)
- Q128 (role permissions)

**Q10 (robot job during pilot) unlocks:**
- Q14 (mission success rate threshold)
- Q52 (shift handoff flow)
- Q60 (floor transition frequency)
- Q99 (waypoint structure, partially auto-answered)
- Q103 (patrol route setup)
- Q106 (post-goal behavior, partially auto-answered)

**Q25 (pilot type: demo/shadow/production) unlocks:**
- Q27 (uptime requirements)
- Q31 (rollback requirement)
- Q65 (existing promises alignment)
- Q66 (safety baseline)
- Q67 (qualitative success signals)

**Q2 (multi-floor transition mechanism) unlocks:**
- Q5 (automatic vs manual floor transitions)
- Q15 (floor transition recovery time)
- Q24 (multi-floor UI representation)
- Q60 (floor transition frequency)
- Q84 (unmapped floor handling, auto-answered)
- Q93 (floor as first-class UI concept, auto-answered)
- Q122 (incremental floor mapping, auto-answered)

**Q26 (real problem: multi-floor vs babysitting) unlocks:**
- Q4 (Phase 1 visible value)
- Q61 (primary success metric)
- Q68 (architecture vs relocalization separation, partially auto-answered)

**Q69 (arise_slam porting risk) unlocks:**
- Q70 (incremental vs simultaneous replacement, partially auto-answered)
- Plan Phase 2 timeline confidence

### Independent Clusters

- **Safety cluster** (Q28, Q66): regulatory + safety baseline, independent of other questions
- **Environment cluster** (Q64, Q18, Q83): site-specific conditions, loosely coupled
- **Support cluster** (Q81, Q89, Q33): knowledge transfer and documentation

---

## Interview Plan

**Round 1: Core Architecture and Business Context** (~8 questions)

These questions unlock the most downstream dependencies. Ask them first.

1. **Q9**: Who is the primary end-user during the factory pilot? *(unlocks 13 downstream questions)*
2. **Q10**: What job is the robot doing during the pilot? *(unlocks 6 downstream questions)*
3. **Q25**: What is the "factory pilot" in business terms? *(unlocks 5 downstream questions)*
4. **Q2**: What does "multi-floor transition" actually mean at the pilot site? *(unlocks 6 downstream questions)*
5. **Q26**: Is the real problem multi-floor operation or babysitting reduction? *(unlocks 3 downstream questions)*
6. **Q61**: Primary success metric -- fewer failed missions, fewer human touches, or faster commissioning?
7. **Q11**: What spaces are in scope and out of bounds?
8. **Q12**: What is explicitly NOT being solved?

**Round 2: Success Criteria and Constraints** (~10 questions, informed by Round 1)

9. **Q14**: Mission completion rate threshold? *(depends on Q10)*
10. **Q27**: Uptime requirement? *(depends on Q25)*
11. **Q16**: Acceptable operator interventions per shift?
12. **Q28**: Safety/compliance/regulatory requirements?
13. **Q65**: Promises already made to stakeholders?
14. **Q4**: Does Phase 1 deliver user-visible value? *(depends on Q26)*
15. **Q60**: How often do missions require floor transitions? *(depends on Q2, Q10)*
16. **Q15**: Floor transition recovery time SLA? *(depends on Q2)*
17. **Q66**: Safety indicators that must not regress?
18. **Q67**: Qualitative success signals?

**Round 3: User Experience Decisions** (~12 questions, informed by Round 1-2)

19. **Q46**: How does operator start navigation? *(depends on Q9)*
20. **Q42**: Most important info at a glance? *(depends on Q9)*
21. **Q40**: Chinese language support required? *(depends on Q9)*
22. **Q53**: Where does the monitoring view live? *(depends on Q9)*
23. **Q54**: Map view default -- 2D or 3D? *(depends on Q9)*
24. **Q45**: Default visible map layers? *(depends on Q9)*
25. **Q24**: Multi-floor visual representation? *(depends on Q2)*
26. **Q5**: Automatic or manual floor transitions? *(depends on Q2)*
27. **Q18**: Behavior when factory layout has changed?
28. **Q103**: Patrol route setup workflow? *(depends on Q10)*
29. **Q86**: Multiple users sending conflicting goals?
30. **Q34**: Recovery actions available without SSH?

**Round 4: Operational and Deployment Details** (~11 questions)

31. **Q52**: Shift handoff flow? *(depends on Q10)*
32. **Q58**: Most painful prior navigation failures?
33. **Q59**: Recovery workflows operators consider "normal"?
34. **Q64**: Environmental conditions that break robots at this site?
35. **Q77**: First-time setup experience?
36. **Q80**: System update mechanism? *(depends on Q9)*
37. **Q31**: Rollback capability required? *(depends on Q25)*
38. **Q33**: New operator onboarding process? *(depends on Q9)*
39. **Q81**: Problem reporting information?
40. **Q89**: Training materials for non-technical staff? *(depends on Q40)*
41. **Q128**: Role-based permissions?

**Round 5: Remaining Standalone Questions** (~11 questions)

42. **Q69**: arise_slam porting -- research or engineering?
43. **Q8**: Existing maps/waypoints migration?
44. **Q13**: AMR competitor benchmarking expectations?
45. **Q95**: Information density for different roles?
46. **Q102**: Operator feedback mechanism for bad paths?
47. **Q4** (revisit if not fully resolved): Phase 1 visible value proposition
48-52: Any remaining questions from Rounds 1-4 that spawned follow-ups

---

**Estimated dialogue:** ~52 questions for user (branch points), ~95 auto-noted (proposed answers to review)

**Recommended approach:** Present Round 1 (8 questions) in a single focused session. Their answers will cascade to auto-resolve several Round 2-3 questions, potentially reducing the remaining dialogue to ~35-40 questions.
