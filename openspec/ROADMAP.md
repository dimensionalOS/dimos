# dimos — high-level roadmap (ruthwik)

One line per track, deliberately terse. Task-level tracking lives on Agent
Board (http://127.0.0.1:4747); detailed specs are created per-feature in the
worktree with `/opsx-propose` and live under `openspec/changes/<name>/`.

## Active

- **Hosted teleop — operator audio to Go2** — branch `ruthwik/hostedteleop/6`,
  worktree `hostedteleop-6`. Finish bridge lifecycle test coverage.
- **Manipulation — GraspGenX sprint** — branch `manip/grasp-sprint-july-26`,
  worktree `grasp-sprint-july-26`. Runtime isolation, HF checkpoint caching.

## Next

- (queue new tracks here before opening a worktree)

## Merged

- Hosted teleop 3 — xArm cloudflare blueprints on the split module pattern.
