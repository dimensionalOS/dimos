# Random-object ACT execution handoff

User authorized implementation and requires ACT to perform grasps. They requested sparse milestone checks during long training, with jobs surviving terminal disconnects. Work is in /home/mustafa/dimos-wt/r1pro-act-sim; root checkout and shared environment stay untouched.

Existing interactive work was committed as 14f90296c6 without coauthor trailers (all hooks passed). A backup branch backup/r1pro-before-random-objects-20260911 preserves it. Rebase onto current origin/main tried to replay 92 dependency commits and conflicted on the isolated Python runtime that main relocated. The rebase was aborted cleanly. Integration with main remains pending; no push this turn.

New implementation (uncommitted): object_packing_scene.py samples 4-5 independently positioned, sized, coloured and weighted boxes, cylinders and compound bottles. Rejection sampling uses bounded whole-layout restarts. Partial tray occupancy is randomized. Geometry remains simulator ground truth. The new overlay uses a 29 cm internal tray at (0.34,-0.04), with clearance reserved for releasing shorter objects below the rim; old bottle scenes stay unchanged.

object_packing.py defines a new 52-feature environment contract: selected object relative to TCP, destination relative to TCP, object orientation/dimensions/shape, home relative to TCP, and sorted neighboring poses/extents with padding masks. Joint targets remain ACT actions. object_packing_task.py shares physical task mechanics, actual bilateral grasp/lift evidence and supported release. It uses the SDK teacher only for collection. Torso yaw is allowed for reach; other torso axes assist lift/clearance, then return before placement. The unused left arm is folded at (0.6,0.1,0,-1.9,0,0,0). Teacher commands with discontinuous joint jumps are rejected. Transit tolerance 3 mm / 0.005 rad; grasp/place tolerance 1 mm / 0.005 rad. Completion still requires physical support, containment, settled motion, release and calibrated joint home.

Teacher pilots 01-05 exposed unused-arm furniture contacts, poor coupled IK routes, short-object release clearance, and unreachable fixed-torso transfers. Pilot 06 succeeded on bottle, box and cylinder picks. Its remaining partial-occupancy failures were false spill reports: non-target bottles moved 0.05-0.14 mm but a momentary zero support force made inside=False. Containment now uses geometry separately; final selected-object success still requires actual support. Pilot 07 is the current wider 8-layout / 2-choice physical regression under recordings/r1pro-act-task/jobs/random-objects-teacher-07. Inspect manifest and log before claiming success.

The image smoke collection under jobs/random-objects-images-smoke uses two paired-choice layouts; three image episodes had passed at this checkpoint. Data processing/initialization/export support the new object profile. demo_object_packing.py evaluates independent held-out seeds and full sequences without any teacher fallback; failed physical evaluations return nonzero and preserve results. A training smoke check and full collection/training have not started yet.

demo_train_objects.py implements a detached, file-locked supervisor: collect, coverage gate, convert, initialize ACT, train/checkpoint, export, evaluate single picks, evaluate sequences. Defaults: 64 layouts, 2 choices/layout, 10,000 updates, batch 32, local-only artifacts, no network, sparse training logs. It keeps paired choices from a layout on the same diagnostic train/validation side; final physical evaluation uses different seeds. It never promotes a checkpoint automatically. Needs workflow smoke validation before launch. Failure/interruption preserves data and logs; collection resumes, but incomplete conversion/training may still require explicit recovery. The process survives terminal disconnects.

Checks: 19 geometry tests passed. Six-file strict mypy passed before the final containment distinction and pipeline split/lock edits. Isolated cached LeRobot environment loads the new profile successfully. Still needed: finish physical pilot, smoke convert/train/export/evaluate, launch the full background cycle, native blueprint/runtime wiring and native learned validation, complete focused checks/handoff/commits, integrate main and publish. No general-object checkpoint is ready.

## First persistent training cycle launched

Physical teacher pilot 07 completed 15/15 attempted picks across eight layouts, all three object families, and 0-3 initially loaded tray objects. Image smoke completed 4/4 paired-choice episodes. Conversion, initialization, five training updates and export passed. The first evaluation attempt revealed an unnecessary RoboPlan dependency in the learned evaluator. SDK initialization now occurs only inside teacher_actions; evaluation-02 ran ACT and wrote a valid 0/1 result after the deliberately short two-second/five-update smoke. This validates the pipeline, not learned task success. See jobs/random-objects-images-smoke/smoke-verification.json.

The full job is recordings/r1pro-act-task/jobs/random-objects-act-v1. Supervisor PID 678343; monitor PID 681775. It has begun collection successfully. It will run 64 layouts with up to two selected targets each, preserve partial occupancy variation, gate teacher coverage, convert, initialize, perform 10,000 ACT updates, export, then test 12 held-out individual picks and eight full sequences. Raw observations are 52-dimensional scene features plus two RGB cameras and 20 measured joints; actions are 20 joint targets. Current candidates/default blueprints are not changed.

progress.json is refreshed by a lightweight independent monitor every five minutes, with no continuous model polling. status.json records supervisor stages; collect.log/train.log/evaluate-*.log contain details. source-provenance.json records hashes for the actual collection implementation. All processes are detached, with no terminal stdin or display dependency. No API key is used. Terminal disconnection does not stop these jobs.

Current focused checks: 38 geometry/recovery/tray checks passed before the additional full-tray assertion; the final object suite passed 20 tests. Six-file strict mypy passed. Strict backend mypy also passed for all four changed integration files. The original bottle physical regression passed after excluding unrelated ROS plugin autoload (one physical grasp test passed). All pre-commit checks passed. The new random-object pipeline still needs a successful learned checkpoint, native ControlCoordinator/blueprint wiring and native evaluation; neither general-object success nor complete multi-stop unloading is validated yet. Main integration remains pending after the cleanly aborted rebase.
