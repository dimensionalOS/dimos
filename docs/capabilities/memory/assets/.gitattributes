# The svgs here are text and want to be diffed and rendered, on github as well
# as on the site; the parent ** rule would put them in LFS, where github serves
# the pointer instead of the drawing. The rasters beside them stay in LFS.
*.svg -filter -diff -merge text
